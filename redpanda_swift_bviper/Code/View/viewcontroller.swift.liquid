{% include 'header' %}

import UIKit

class {{ module_info.name }}ViewController: UIViewController {

    var presenter: {{ module_info.name }}Presenter?
    var viewModel: {{ module_info.name }}ViewModel?

    override func viewDidLoad() {
        super.viewDidLoad()
        self.presenter?.loadContent()
    }

    // MARK: Actions

    func sampleAction() {
        self.presenter?.performSampleAction()
    }

}

// MARK: - <{{ module_info.name }}View>
extension {{ module_info.name }}ViewController: {{ module_info.name }}View {

    func displayNoData() {
        self.viewModel = nil
    }

    func displayData(_ viewModel: {{ module_info.name }}ViewModel) {
        self.viewModel = viewModel
    }

}
